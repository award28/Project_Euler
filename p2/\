#include <iostream>
using namespace std;

int main() {
    int sum = 0; 
    int fib = 1;

    while( fib < 4000000) {
        if(fib %2 == 0) sum += fib;
        fib+=1;
    }

    cout << sum << endl;
}
