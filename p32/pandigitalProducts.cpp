#include <iostream>
using namespace std;

//int numberLetterCounter(int range);

int main() {
    bool finished = false;
    int x = 1;
    int y = 2;
    int z = 2;
    string 
    while(!finished) {
        x = getUniqueNum(x);
        y = getUniqueNum(y);
        z = x * y;
         
    }
}

//int numberLetterCounter(int range) {

/*
 * Functions to be made:
 *  Are two ints different and each char unique? 
 *  Take two ints and their product, put them in a string. Is it exactly 9 digits long?
 *  Does this contain 1 - 9?
 *  
 */
